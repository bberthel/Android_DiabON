<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.android.imac.diabon" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppBaseTheme" >
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".NavigationDrawer" />
        <activity
            android:name=".CarnetActivity"
            android:label="@string/title_activity_carnet" />
        <activity
            android:name=".AlimentationActivity"
            android:label="@string/title_activity_alimentation" />
        <activity
            android:name=".ConseilsActivity"
            android:label="@string/title_activity_conseils" />
    </application>

</manifest>
